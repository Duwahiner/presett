# Delta for dashboard-page

## ADDED Requirements

### Requirement: Dashboard hierarchy follows the reference

The system MUST restyle the dashboard page to match the v2 hierarchy and control treatment while preserving all dashboard workflows.

#### Scenario: Dashboard content keeps behavior
- GIVEN the user opens the dashboard
- WHEN quick actions, cards, and status areas render
- THEN the existing workflow remains available
- AND the page uses square surfaces and compact mono labels
